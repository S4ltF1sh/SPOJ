//P135SUMG - SUM5 G - Xâu đặc biệt
//Trong bảng chữ cái tiếng anh có 5 nguyên âm là A, E, I, O, U. Các chữ cái còn lại là phụ âm.

#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>
#include <array>
using namespace std;

bool isNA(char c)
{
    if (c == 'A' || c == 'E' || c == 'I' || c == 'O' || c == 'U')
        return true;
    return false;
}

bool Check(string s)
{
    
}